Yrgopelag is developed in another time and the main focus for now is to rewrite the code. 

Rewrite the code: The code should be in OOP instead of plain PHP. Use the agent to find things to de better and fix the problems. 

Goal: The goal is to rewrite the code from plain php to OOP. 

You and the agent should only be in this branch (claude). 